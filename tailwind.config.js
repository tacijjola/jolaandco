module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
  theme: {
    extend: {
      colors: {
        navySlate:   "#2D3644",   // dark navy — text, header, buttons
        goldenWheat: "#9F8551",   // warm gold — accents, borders, highlights
        lightStone:  "#FFFFFF",   // white — main page background
        sandBeige:   "#EDE6D6",   // warm sand — alternating section backgrounds
        sage:        "#6B8F71",   // muted green — hover states, subtle accents
      },
    },
  },
  plugins: [],
};
